---
apiVersion: v1
kind: ConfigMap
metadata:
  name: openwrt-etc
  namespace: openwrt
  labels:
    app.kubernetes.io/name: openwrt
data:
  # TODO: populate with OpenWrt base config files as needed
  # e.g. /etc/config/network, /etc/config/firewall
  placeholder: "# Add OpenWrt config files here"
