---
apiVersion: bitnami.com/v1alpha1
kind: SealedSecret
metadata:
  name: jellyfin-secrets
  namespace: jellyfin
  labels:
    app.kubernetes.io/name: jellyfin
spec:
  encryptedData:
    # TODO: run `kubeseal` to populate with real ciphertext
    # Example: kubeseal --cert=sealed-secrets-public-key.pem --format yaml \
    #          < /tmp/jellyfin-secrets-plain.yaml > apps/jellyfin/sealedsecret.yaml
    JELLYFIN__DEFAULTADMINPASSWORD: ""
  template:
    metadata:
      name: jellyfin-secrets
      namespace: jellyfin
    type: Opaque
